<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:id="@+id/main"
    android:padding="24dp"
    android:background="@color/white">

    <!-- ✅ App Logo -->
    <ImageView
        android:id="@+id/appLogo"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@mipmap/ic_launcher"
        android:contentDescription="App Logo" />

    <!-- ✅ Rotating Titles (Changing Texts) -->
    <TextView
        android:id="@+id/rotatingTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to Smart Food Inventory!"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="32dp"
        android:textColor="@color/green"
        android:textAlignment="center" />

    <!-- ✅ Rotating Descriptions -->
    <TextView
        android:id="@+id/rotatingDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Manage your food inventory efficiently."
        android:textSize="14sp"
        android:layout_marginTop="8dp"
        android:textAlignment="center"
        android:textColor="@color/black" />

    <!-- ✅ Login Button -->
    <Button
        android:id="@+id/loginButton"
        android:layout_width="181dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:backgroundTint="@color/green"
        android:text="Login"
        android:textColor="@android:color/white" />

    <!-- ✅ Sign Up Button -->
    <Button
        android:id="@+id/signUpButton"
        android:layout_width="181dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:backgroundTint="@color/purple"
        android:text="Sign Up"
        android:textColor="@android:color/white" />

</LinearLayout>
